#include<stdio.h>
int main() {
    
    // char str[20];
    // scanf("%s", &str);
    // printf("%s", str);

    char str[20];
    scanf("%c",&str);
    printf("%s",str);
    
    // char str[50];
    // fgets(str, sizeof(str), stdin);
    // fputs(str, stdout);
    // printf("\n%c\n", str[0]);
    // char s[20]; 
    // gets(s);
    // fputs(s, stdout);
    

    // char str[20];
    // scanf("%s",&str);
    // printf("%s",str);

    return 0;
}